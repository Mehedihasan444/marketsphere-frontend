import { Outlet } from "react-router-dom";

const Dashboard = () => {
  return (
    <div>
      {/* sidebar */}
      <>
      </>
      {/* main content */}
      <Outlet />
    </div>
  );
};

export default Dashboard;
